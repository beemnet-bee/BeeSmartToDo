# Run and deploy

This contains everything you need to run the app locally.

View the app on: https://bee-smart-to-do.vercel.app/

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
